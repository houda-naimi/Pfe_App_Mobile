<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:PfeShell.Views"
       xmlns:fontAwesome="clr-namespace:FontAwesome"
       xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
       Title="PfeShell"
       x:Class="PfeShell.AppShell"
       >

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->
    

    <Shell.Resources>
        <ResourceDictionary>
            <Color   x:Key="BGcolor">#A0A4C9</Color>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="LightGray" />
                <Setter Property="Shell.ForegroundColor" Value="#6670CC" />
                <Setter Property="Shell.TitleColor" Value="Black" />
                <Setter Property="Shell.DisabledColor" Value="Black" />
                <Setter Property="Shell.UnselectedColor" Value="Black"  />
                <Setter Property="Shell.TabBarBackgroundColor"  Value="{StaticResource BGcolor}"   />
                <Setter Property="Shell.TabBarForegroundColor" Value="#6670CC"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="White"/>
                <Setter Property="Shell.TabBarTitleColor" Value="#6670CC"/>
                
              
            </Style>
            <Style TargetType="TabBar"  BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />

            <!--
            Default Styles for all Flyout Items
            https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#flyoutitem-and-menuitem-style-classes
            -->
            <Style Class="FlyoutItemLabelStyle" TargetType="Label">
                <Setter Property="TextColor" Value="Black"></Setter>
                
               
            </Style>
            <Style Class="FlyoutItemLayoutStyle" TargetType="Layout" ApplyToDerivedTypes="True">
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{x:OnPlatform UWP=Transparent, iOS=White}" />
                                 
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="#6670CC" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="UnSelected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!--
            Custom Style you can apply to any Flyout Item
            -->
            <Style Class="MenuItemLayoutStyle" TargetType="Layout" ApplyToDerivedTypes="True">
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter TargetName="FlyoutItemLabel" Property="Label.TextColor" Value="{StaticResource Primary}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- 
        When the Flyout is visible this defines the content to display in the flyout.
        FlyoutDisplayOptions="AsMultipleItems" will create a separate flyout item for each child element    
        https://docs.microsoft.com/dotnet/api/xamarin.forms.shellgroupitem.flyoutdisplayoptions?view=xamarin-forms
    -->

    <ShellItem Route="Login" FlyoutItemIsVisible="False">
        <ShellContent ContentTemplate="{DataTemplate local:Login}" />
    </ShellItem>
    <ShellItem Route="Authentification" FlyoutItemIsVisible="False">
        <ShellContent ContentTemplate="{DataTemplate local:Authentification}" />
    </ShellItem>

    <FlyoutItem Title="Prospection">
        <FlyoutItem.Icon>
        <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.Users}" Color="DarkBlue"></FontImageSource>
        </FlyoutItem.Icon>
        <Tab Title="Appel" >
        <Tab.Icon>
        <FontImageSource FontFamily="FAS"
                     Glyph="{x:Static fontAwesome:FontAwesomeIcons.PhoneAlt}"></FontImageSource>
        </Tab.Icon>
            <ShellContent Route="ListeApplTelephonique" ContentTemplate="{DataTemplate local:ListeApplTelephonique}" />
        </Tab>
        <Tab Title="Prospect">
            <Tab.Icon>
                <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.AddressBook}"></FontImageSource>
            </Tab.Icon>
            <ShellContent Route="ListeProspects" ContentTemplate="{DataTemplate local:ListeProspects}" />
        </Tab>
        <Tab Title="Disponible">
            <Tab.Icon>
                <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.HouseUser}"></FontImageSource>
            </Tab.Icon>
            <ShellContent Route="Disponible" ContentTemplate="{DataTemplate local:Disponible}" />
        </Tab>
        <Tab Title="Calendrier">
            <Tab.Icon>
                <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.CalendarAlt}"></FontImageSource>
            </Tab.Icon>
            <ShellContent Route="Agenda" ContentTemplate="{DataTemplate local:Agenda}" />
        </Tab>
        <Tab Title="Echéance">
            <Tab.Icon>
                <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.DollarSign}"></FontImageSource>
            </Tab.Icon>
            <ShellContent Route="EcheancePage" ContentTemplate="{DataTemplate local:EcheancePage}" />
        </Tab>
 
    </FlyoutItem>
    <FlyoutItem Title="Service Aprés Vente">
        <FlyoutItem.Icon>
            <FontImageSource FontFamily="FAS"
                         Glyph="{x:Static fontAwesome:FontAwesomeIcons.UniversalAccess}" Color="DarkBlue"></FontImageSource>
        </FlyoutItem.Icon>
        <Tab Title="Reclamation">
            <ShellContent Route="AffichReclamation" ContentTemplate="{DataTemplate local:AffichReclamation}" />
        </Tab>
        

    </FlyoutItem>
   <!-- <FlyoutItem Title="Test">
        <Tab Title="Test">
            <ShellContent Route="Test" ContentTemplate="{DataTemplate local:Test}" />
        </Tab>
    </FlyoutItem> -->

    <!-- When the Flyout is visible this will be a menu item you can tie a click behavior to  
    <MenuItem Text="Prospection" StyleClass="MenuItemLayoutStyle" Clicked="MenuItem_Clicked">
    </MenuItem>-->

    <!--
        TabBar lets you define content that won't show up in a flyout menu. When this content is active
        the flyout menu won't be available. This is useful for creating areas of the application where 
        you don't want users to be able to navigate away from. If you would like to navigate to this 
        content you can do so by calling 
        await Shell.Current.GoToAsync("//LoginPage");
    -->
    <!--
    <TabBar>
        <ShellContent Route="LoginPage" ContentTemplate="{DataTemplate local:LoginPage}" />
    </TabBar>
-->
    <!-- Optional Templates 
    // These may be provided inline as below or as separate classes.

    // This header appears at the top of the Flyout.
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#flyout-header
    <Shell.FlyoutHeaderTemplate>
        <DataTemplate>
            <Grid>ContentHere</Grid>
        </DataTemplate>
    </Shell.FlyoutHeaderTemplate>

    // ItemTemplate is for ShellItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-flyoutitem-appearance
    <Shell.ItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Title, Icon
            </ContentView>
        </DataTemplate>
    </Shell.ItemTemplate>

    // MenuItemTemplate is for MenuItems as displayed in a Flyout
    // https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/flyout#define-menuitem-appearance
    <Shell.MenuItemTemplate>
        <DataTemplate>
            <ContentView>
                Bindable Properties: Text, Icon
            </ContentView>
        </DataTemplate>
    </Shell.MenuItemTemplate>

    -->

</Shell>
